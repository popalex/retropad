// Preferences load/save for retropad
#pragma once

/* Load preferences from ~/.config/retropad/config.ini and apply them */
void LoadPrefs(void);

/* Save current preferences to ~/.config/retropad/config.ini */
void SavePrefs(void);
